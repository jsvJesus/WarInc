sampler source 	: register(s0);

float4 main(float2 texCoord : TEXCOORD0): COLOR
{
	float4 color = tex2D(source, texCoord);
	color.rgb = sqrt(color.rgb);
	color.a = dot(float3(0.299, 0.587, 0.114), color.rgb);
	return color;
}
