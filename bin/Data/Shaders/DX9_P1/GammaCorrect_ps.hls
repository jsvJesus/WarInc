float4 vConsts : register( c0 );

sampler2D sTex : register( s0 );

float4 main( in float4 vTexc : TEXCOORD0 ) : COLOR0
{
	float4 vColr = tex2D( sTex, vTexc.xy );
	vColr.rgb = pow( vColr.rgb, vConsts.x );
	return  vColr;
}