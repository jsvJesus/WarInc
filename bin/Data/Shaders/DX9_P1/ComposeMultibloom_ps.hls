//

sampler source[NUM_SOURCE_IMAGES]: register(s0);

float4 main(float2 texCoord : TEXCOORD0): COLOR
{
	float4 cl = 0;
	for (int i = 0; i < NUM_SOURCE_IMAGES; ++i)
	{
		cl += tex2D(source[i], texCoord);
	}
	
	return cl / NUM_SOURCE_IMAGES;
}
